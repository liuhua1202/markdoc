保留此目录结构,文件由 CI 同步脚本注入:

```bash
# 同步根目录文件到 Android assets/
cp ../../index.html .
cp ../../manifest.json .
cp ../../sw.js .
cp ../../logo.svg .
cp -r ../../icons .
cp -r ../../vendor .
```

详见仓库根的 `.github/workflows/release.yml` 的 `prepare` job。
